const { defineConfig } = require("cypress");

  module.exports = {
  projectId: "z967ev",
  e2e: {
    setupNodeEvents(on, config) {
      // implement node event listeners here
 //   },
      // SUAS OUTRAS CONFIGURAÇÕES EXISTENTES AQUI...
   // experimentalStudio: true // ← ADICIONE ESTA LINHA
//  },
     // implement node event listeners here
  //   setupNodeEvents(on, config) {
  }

}
  }